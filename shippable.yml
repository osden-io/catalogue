build_environment: clojure
language: clojure
lein: lein2

before_script:
  - mkdir -p shippable/buildoutput

after_script:
  - cp -v buildoutput/uberjar/catalogue.jar ./shippable/buildoutput/catalogue.jar