build_environment: clojure
language: clojure
lein: lein2

before_script:
  - mkdir -p shippable/buildoutput

script:

after_script:
  - cp -vr target/uberjar/catalogue.jar ./shippable/buildoutput/catalogue.jar