build_environment: clojure
language: clojure
lein: lein2

before_script:
  - mkdir -p buildoutput
  - mkdir -p testresults
  - mkdir -p codecoverage

script:
  - lein test
  - lein uberjar

after_script:
  - cp -v buildoutput/catalogue-0.0.1-SNAPSHOT-standalone.jar ./shippable/buildoutput/catalogue.jar